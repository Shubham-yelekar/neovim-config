# macOS
.DS_Store
._*

# Windows
Thumbs.db
desktop.ini

# Accidental editor dumps
nvim.log

# Shell history - personal, churns on every command, and this repo is public
zsh/.zsh_history
zsh/.zhistory

# TPM installs plugins here. Clone tpm on a fresh machine; do not commit it.
mac/tmux/plugins/

# Background images - 38MB of JPEGs that wezterm.lua never references.
win/wezterm/bg/
